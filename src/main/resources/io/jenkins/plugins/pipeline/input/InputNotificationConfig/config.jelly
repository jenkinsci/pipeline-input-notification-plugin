<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:section title="${%Pipeline Input Notification Configuration}">
        <f:entry title="${%Notifiers}">
            <f:repeatableHeteroProperty field="inputNotifiers" targetType="io.jenkins.plugins.pipeline.input.notifiers.InputNotifier"
                                        addCaption="${%Add Notifier Options}" hasHeader="true" honorOrder="true" />
        </f:entry>
    </f:section>
</j:jelly>
